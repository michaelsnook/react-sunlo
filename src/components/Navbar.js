import React from 'react';

const Navbar = () => (
  <nav className="bg-dark text-white px-5 py-3">
    <div className="container">
      this is a navbar
    </div>
  </nav>
);

export default Navbar;
